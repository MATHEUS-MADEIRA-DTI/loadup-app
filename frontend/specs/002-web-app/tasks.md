# Tasks: LoadUp Frontend Web Application

**Input**: Design documents from `specs/002-web-app/`  
**Prerequisites**: plan.md ✅, spec.md ✅, research.md ✅, data-model.md ✅, contracts/api.md ✅

**Tests**: No tests of any kind (unit, integration, e2e) — per LoadUp Frontend Constitution §UI/UX Standards.

**Organization**: Tasks grouped by user story to enable independent implementation and testing.

---

> **⚠️ ARCHITECTURE RULE** — Every task that creates a page or component MUST:
>
> - Create a `styles.ts` file for all styled components — no styled components inside `page.tsx` or component `index.tsx` files
> - Extract every sub-component into its own folder inside `components/`, each with `index.tsx` + `styles.ts`
> - Keep `page.tsx` under **80 lines** (data fetching hooks + layout composition only — no styled components, no inline components)
> - Keep any component file under **150 lines** — split into smaller components if exceeded
>
> **STOP if any single file exceeds 150 lines** — split before continuing to the next task.

---

## Phase 1: Setup

**Purpose**: Bootstrap the Next.js project with all dependencies and strict TypeScript configuration.

- [x] T001 Bootstrap Next.js 14 project with App Router: `npx create-next-app@latest . --typescript --app --no-tailwind --eslint --src-dir --import-alias "@/*"` in `C:\Users\dti-\Documents\LoadUp\frontend`
- [x] T002 Install runtime dependencies: `npm install styled-components @tanstack/react-query recharts lucide-react`
- [x] T003 Install dev dependencies: `npm install -D @types/styled-components`
- [x] T004 Verify `tsconfig.json` has `"strict": true`, `"noImplicitAny": true`, `"strictNullChecks": true` in `tsconfig.json`
- [x] T005 Create `.env.local` with `NEXT_PUBLIC_API_URL=http://localhost:3000`
- [x] T006 Create source directories: `src/components`, `src/services`, `src/hooks`, `src/styles`, `src/types`, `src/lib`, `src/constants`
- [x] T007 Delete boilerplate files generated by create-next-app that will be replaced: `src/app/globals.css` content (keep file, clear content), remove default page content from `src/app/page.tsx`
- [x] T008 Configure Styled Components SSR for Next.js 14 App Router:
  - Add `compiler: { styledComponents: true }` to `next.config.js` (or `next.config.ts`)
  - Create `src/lib/registry.tsx`: `'use client'` component that uses `useServerInsertedHTML` from `next/navigation` and `ServerStyleSheet` from `styled-components` to collect and inject styles server-side, preventing FOUC. Export as `StyledComponentsRegistry`.
  - Wrap `{children}` in `src/app/layout.tsx` with `<StyledComponentsRegistry>` so all styled components render correctly on the server before any hydration.

**Checkpoint**: `npm run dev` starts without errors. TypeScript strict mode active. No flash of unstyled content on first load.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Theme system, token storage, API client, query client, global styles, and all TypeScript types. Nothing else can be built until these are complete.

**⚠️ CRITICAL**: No user story work can begin until this phase is complete.

### 2A — Theme System

- [x] T009 Create `src/styles/theme.ts` with the `AppTheme` TypeScript interface and both `darkTheme` and `lightTheme` objects. Include all tokens from data-model.md: `colors` (all 18 keys including `muscleGroups` for all 8 groups), `typography` (7 scales), `spacing` (6 steps), `borderRadius` (6 values), `shadows` (2 values). Figma reference: file `Buolm5kzeDIg7FMTyM21r5` — primary `#6750A4`, surface `#FFFBFE` / dark surface `#1C1B1F`, glass overlay `rgba(255,255,255,0.15)`, card radius 48px, inner radius 36px, pill 1200px.
- [x] T010 Create `src/styles/ThemeProvider.tsx`: define `ThemeContext` with `{ theme: AppTheme; isDark: boolean; toggleTheme: () => void }`, `ThemeProvider` component that reads `loadup_theme` from `localStorage` on mount, wraps children with Styled Components `ThemeProvider` and persists toggle to `localStorage`; export `useTheme` hook.
- [x] T011 Create `src/styles/globalStyles.ts` with Styled Components `createGlobalStyle`: CSS reset (box-sizing, margin/padding 0), `font-family: 'Roboto', sans-serif`, `background-color: ${({ theme }) => theme.colors.background}`, `color: ${({ theme }) => theme.colors.onSurface}`, no overflow-x, smooth scroll.

### 2B — Foundation Utilities

- [x] T012 [P] Create `src/lib/tokenStorage.ts`: `TOKEN_KEY = 'loadup_token'`, export `tokenStorage` object with `get(): string | null`, `set(token: string): void`, `clear(): void` — all using `localStorage`.
- [x] T013 [P] Create `src/lib/apiClient.ts`: generic `request<T>` function accepting `path: string` and `RequestInit`. Reads token via `tokenStorage.get()`, injects `Authorization: Bearer` header, sets `Content-Type: application/json`, uses `NEXT_PUBLIC_API_URL` env var as base. On 401: calls `tokenStorage.clear()` and redirects to `/login`. On non-ok: parses JSON error and throws `Error(body.message)`. Export `apiClient` with typed `get<T>`, `post<T>`, `patch<T>`, `delete<T>` methods.
- [x] T014 [P] Create `src/lib/queryClient.ts`: create and export a singleton `QueryClient` with `defaultOptions.queries.retry: 1`, `staleTime: 30_000`, `gcTime: 5 * 60_000`.

### 2C — TypeScript Types

- [x] T015 [P] Create `src/types/user.ts` with interfaces: `User` (`id`, `email`, `timezone?`, `createdAt`), `AuthResponse` (`id`, `email`, `token`, `expiresIn`, `createdAt`), `LoginPayload`, `RegisterPayload` — per data-model.md.
- [x] T016 [P] Create `src/types/trainingSheet.ts` with all interfaces: `DayOfWeek` (union), `DayType` (union), `SeriesType` (union), `MuscleGroup` (union of 8 groups), `Series`, `Exercise`, `TrainingDay`, `TrainingSheet`, `CreateExercisePayload`, `UpdateExercisePayload` — per data-model.md.
- [x] T017 [P] Create `src/types/trainingSession.ts` with: `SessionStatus`, `LoggedSet` (`_id`, `exerciseName`, `seriesType`, `seriesOrder`, `weight`, `repsCompleted`, `restTime`), `TrainingSession`, `AddRecordPayload`, `UpdateRecordPayload`, `CompleteSessionPayload` — per corrected data-model.md.
- [x] T018 [P] Create `src/types/calendar.ts` with: `CalendarDayStatus`, `CalendarDay`, `TodayCalendar`, `MonthlyCalendar`, `WeeklyCalendar` — per data-model.md.
- [x] T019 [P] Create `src/types/progression.ts` with: `ProgressionPoint`, `ExerciseChart`, `PersonalRecord`, `ProgressionSummary` — per data-model.md.
- [x] T020 Create `src/types/index.ts` that re-exports all types from the 5 type files above.

### 2D — String Constants

- [x] T021 Create `src/constants/strings.ts` as a `const` object with all UI strings in pt-BR, namespaced by screen. Must include namespaces: `auth` (login/register labels, errors, button text), `home` (greeting, rest day, workout labels, mark completed/skipped), `trainingPlan` (sheet title, day names, toggle labels, empty state), `exercises` (list title, add/edit/delete labels, empty state, muscle groups), `workout` (screen title, set labels, finish/skip session, read-only notice), `progression` (chart title, summary labels — streak, sessions, PR), `nav` (tab labels: Início, Treinos, Treinar, Progresso), `common` (loading, error, retry, confirm, cancel, save).

### 2E — Services Layer

- [x] T022 [P] Create `src/services/authService.ts`: `register(payload: RegisterPayload): Promise<AuthResponse>` → `POST /auth/register`; `login(payload: LoginPayload): Promise<AuthResponse>` → `POST /auth/login`. On success, caller must store token.
- [x] T023 [P] Create `src/services/userService.ts`: `getMe(): Promise<User>` → `GET /users/me` via `apiClient.get`.
- [x] T024 [P] Create `src/services/trainingSheetService.ts`: `getTrainingSheet(): Promise<TrainingSheet>`, `createTrainingSheet(): Promise<TrainingSheet>` → `POST /training-sheet`, `updateDay(day: DayOfWeek, status: DayType): Promise<TrainingSheet>` → `PATCH /training-sheet/days/:day` with body `{ status }`.
- [x] T025 [P] Create `src/services/exerciseService.ts`: `getExercises(day: DayOfWeek): Promise<Exercise[]>`, `addExercise(day: DayOfWeek, payload: CreateExercisePayload): Promise<TrainingSheet>`, `updateExercise(day: DayOfWeek, id: string, payload: UpdateExercisePayload): Promise<TrainingSheet>`, `deleteExercise(day: DayOfWeek, id: string): Promise<void>` → `DELETE` (204 no content, return void).
- [x] T026 [P] Create `src/services/sessionService.ts`: `getTodaySession(): Promise<TrainingSession>`, `createSession(date: string): Promise<TrainingSession>`, `addRecord(sessionId: string, payload: AddRecordPayload): Promise<TrainingSession>` → `POST /training-sessions/:sessionId/records`, `updateRecord(sessionId: string, recordId: string, payload: UpdateRecordPayload): Promise<TrainingSession>` → `PATCH /training-sessions/:sessionId/records/:recordId`, `deleteRecord(sessionId: string, recordId: string): Promise<TrainingSession>`, `completeSession(sessionId: string, payload: CompleteSessionPayload): Promise<TrainingSession>` → `PATCH /training-sessions/:sessionId/complete`.
- [x] T027 [P] Create `src/services/calendarService.ts`: `getToday(): Promise<TodayCalendar>`, `getMonthly(year: number, month: number): Promise<MonthlyCalendar>`, `getWeekly(): Promise<WeeklyCalendar>`.
- [x] T028 [P] Create `src/services/progressionService.ts`: `getSummary(): Promise<ProgressionSummary>`, `getChart(exerciseName: string): Promise<ExerciseChart>` → `GET /progression/chart/${encodeURIComponent(exerciseName)}`.

### 2F — React Query Hooks

- [x] T029 [P] Create `src/hooks/useAuth.ts`: `useLogin()` mutation (stores token on success, invalidates all queries, navigates to `/`), `useRegister()` mutation (on success does **not** navigate — calls a provided `onSuccess` callback so the auth page can switch to the login tab and show a success message), `useLogout()` function (clears token, calls `queryClient.clear()`, navigates to `/login`).
- [x] T030 [P] Create `src/hooks/useTrainingSheet.ts`: `useTrainingSheet()` query (key `['training-sheet']`), `useCreateTrainingSheet()` mutation (invalidates `['training-sheet']`), `useUpdateDay()` mutation (invalidates `['training-sheet']`).
- [x] T031 [P] Create `src/hooks/useExercises.ts`: `useExercises(day: DayOfWeek)` query (key `['training-sheet', 'day', day, 'exercises']`), `useAddExercise(day)` mutation, `useUpdateExercise(day)` mutation, `useDeleteExercise(day)` mutation. All mutations invalidate `['training-sheet']` and `['training-sheet', 'day', day, 'exercises']`.
- [x] T032 [P] Create `src/hooks/useSession.ts`: `useTodaySession()` query (key `['training-sessions', 'today']`), `useCreateSession()` mutation, `useAddRecord(sessionId)` mutation, `useUpdateRecord(sessionId)` mutation, `useDeleteRecord(sessionId)` mutation, `useCompleteSession(sessionId)` mutation. All mutations invalidate `['training-sessions', 'today']`.
- [x] T033 [P] Create `src/hooks/useCalendar.ts`: `useTodayCalendar()` (key `['calendar', 'today']`), `useMonthlyCalendar(year, month)` (key `['calendar', 'monthly', year, month]`), `useWeeklyCalendar()` (key `['calendar', 'weekly']`).
- [x] T034 [P] Create `src/hooks/useProgression.ts`: `useProgressionSummary()` (key `['progression', 'summary']`), `useProgressionChart(exerciseName: string)` (key `['progression', 'chart', exerciseName]`, enabled only when `exerciseName` is non-empty).

### 2G — Root Layout & Shared Wrappers

- [x] T035 Update `src/app/layout.tsx` (root layout): wrap children with `StyledComponentsRegistry` (from `src/lib/registry.tsx`, T008), then `ThemeProvider` (from `src/styles/ThemeProvider.tsx`), inject `GlobalStyles`, wrap with `QueryClientProvider` using singleton `queryClient`. Set `<html lang="pt-BR">`. Import Roboto from `next/font/google` (weights 400, 500, 600, 700) and apply CSS variable to `<body>`. Nesting order: `StyledComponentsRegistry` → `ThemeProvider` → `GlobalStyles` → `QueryClientProvider` → `{children}`.

### 2H — Shared Components

- [x] T036 Create `src/components/BottomNavBar/index.tsx`: Styled Component fixed to bottom, height 80px, `background: ${theme.colors.surface}`, 4 nav items (Início `/`, Treinos `/training-plan`, Treinar `/train`, Progresso `/progress`). Active tab determined by `usePathname()`. Active icon color `theme.colors.primary`, inactive `theme.colors.outline`. Labels via `strings.nav`. All styles via theme tokens only.
- [x] T037 Create `src/components/StatCard/index.tsx`: glassmorphism card (background `theme.colors.glassOverlay`, `backdrop-filter: blur(10px)`, border-radius `theme.borderRadius.inner`). Props: `icon`, `label: string`, `value: string | number`, `unit?: string`. Labels and values use typography tokens. Mobile-first sizing.
- [x] T038 Create `src/components/MuscleChip/index.tsx`: inline badge pill. Props: `muscleGroup: MuscleGroup`. Background and text color derived from `theme.colors.muscleGroups[muscleGroup]`. Border-radius `theme.borderRadius.chip`. Font: `theme.typography.labelSmall`. No hardcoded colors.
- [x] T039 Create `src/components/ExerciseCard/index.tsx`: card with exercise name, `MuscleChip`, and a list of series rows. Props: `exercise: Exercise`, `onEdit?: () => void`, `onDelete?: () => void`. Styled with `theme.borderRadius.inner`, surface background. Series displayed via `SeriesRow` component.
- [x] T040 Create `src/components/SeriesRow/index.tsx`: single row for a planned series. Props: `series: Series`, `index: number`. Displays type badge (warm-up/adjustment/working) and rep count. All labels via `strings.exercises`. Styled with theme typography tokens.
- [x] T041 Create `src/components/CalendarGrid/index.tsx`: monthly grid (7 columns × ~5 rows). Props: `calendar: MonthlyCalendar`, `onMonthChange: (year: number, month: number) => void`. Each day cell shows date number and a status indicator dot (completed=`theme.colors.successContainer`, skipped=`theme.colors.errorContainer`, pending=`theme.colors.outline`, rest=transparent). Today's cell highlighted. Mobile-first: cell minimum 40px.
- [x] T042 Create `src/components/EmptyState/index.tsx`: centered layout with an optional icon, a title, a description, and an optional CTA button. Props: `title: string`, `description?: string`, `ctaLabel?: string`, `onCta?: () => void`. Styled with theme typography and spacing tokens.
- [x] T041B Create `src/lib/animations.ts`: export four Styled Components `keyframes` objects — `pageEnter` (fade-in + translate Y 12px→0, 300ms ease), `tabEnter` (fade-in + translate X 8px→0, 200ms ease-out), `modalEnter` (fade-in + scale 0.95→1, 200ms ease-out), `modalOverlay` (opacity 0→0.5, 150ms linear). Export typed transition config constants `PAGE_TRANSITION`, `TAB_TRANSITION`, and `MODAL_TRANSITION` (duration + easing strings) for use in `transition` CSS properties. No runtime dependencies; pure Styled Components `keyframes`.
- [x] T041C Create `src/components/PageTransition/index.tsx`: `'use client'` wrapper component. Renders a single `<div>` styled with `animation: ${pageEnter} 300ms ease both`. Apply as the outermost JSX element inside every `(app)/*` page component (home, training-plan, train, progress). Does **not** apply to layouts, auth pages, or modals.
- [x] T041D Create `src/components/Modal/index.tsx`: `'use client'` base modal component. Props: `isOpen: boolean`, `onClose: () => void`, `children: React.ReactNode`, `title?: string`. Renders a fixed full-screen overlay (`modalOverlay` keyframe, `background: rgba(0,0,0,0.5)`, `z-index: 100`) and a centered content card (`modalEnter` keyframe, `background: theme.colors.surface`, `borderRadius: theme.borderRadius.inner`, `padding: theme.spacing.lg`, max-width 480px, 90vw). Includes a close button (top-right ×) that calls `onClose`. Traps focus when open (`autoFocus` on close button). Returns `null` when `isOpen` is false. All feature modals import and use this component.
- [x] T041E Replace emoji strings with `lucide-react` icons throughout the app. Install: `npm install lucide-react`. Swap raw emoji characters in JSX for semantically appropriate Lucide icon components — import icons individually from `lucide-react` (e.g. `import { Flame, Dumbbell, Moon, CheckCircle, SkipForward } from 'lucide-react'`). Size and color must be passed as props tied to theme tokens (never hardcoded). Apply to: BottomNavBar nav icons (T036), StatCard stat icons (T037), SeriesRow series-type icons (T040), Home screen rest-day and session-status icons (T046–T048), Workout screen set-status icons (T057–T061), Progress screen section icons (T062). All icon sizes must respect the minimum 44×44 px tap-target rule; wrap icons in a sized container when the icon itself is smaller.

**Checkpoint**: Foundation complete — all types, services, hooks, and shared components exist. User story screens can now be built independently.

---

## Phase 3: User Story 1 — Authentication (Priority: P1) 🎯 MVP

**Goal**: Users can register, log in, remain authenticated across sessions, and are redirected appropriately.

**Independent Test**: Open the app at `/login`, register a new account, be redirected to `/` (home), close and reopen the browser, still be on home (token persisted).

- [x] T043 [US1] Rewrite `src/app/(auth)/login/page.tsx` as a single auth page with tab switching:
  - Local state: `tab: 'login' | 'register'` (default `'login'`) and `registerSuccess: boolean`
  - Tab bar: two `<button>` elements ("Entrar" / "Criar conta") that call `setTab`. The active underline indicator uses `position: absolute; bottom: -1px; left: 0/50%; width: 50%; transition: left 200ms ease` to slide smoothly between positions.
  - Content area: `<StyledTabContent key={tab}>` — keyed by `tab` so switching remounts the div and replays `tabEnter` (200ms ease-out) from `src/lib/animations.ts`.
  - Login form: email + password inputs (floating labels, SVG icons, eye toggle) + submit. On submit call `useLogin()`. Navigate to `/` on success.
  - Register form: same inputs + submit. On submit call `useRegister({ onSuccess: () => { setTab('login'); setRegisterSuccess(true); } })`. On switch-back, show a `strings.auth.registerSuccess` banner above the login form.
  - Error state above the active form; loading state on submit button. All labels from `strings.auth`.
    **Files to create:**
  - `src/app/(auth)/login/page.tsx` — max 80 lines: tab state + layout composition only
  - `src/app/(auth)/login/styles.ts` — all styled components for the auth page
  - `src/app/(auth)/login/components/LoginForm/index.tsx` — login form fields + submit logic
  - `src/app/(auth)/login/components/LoginForm/styles.ts` — LoginForm styled components
  - `src/app/(auth)/login/components/RegisterForm/index.tsx` — register form fields + submit logic
  - `src/app/(auth)/login/components/RegisterForm/styles.ts` — RegisterForm styled components
- ~~T044~~ _(removed — register is now a tab inside `/login`, route `/register` no longer exists)_
- [x] T045 [US1] `src/app/(auth)/layout.tsx` (auth shell): gradient header + `StyledBody` wrapper. Static shell only — no route-based animation needed.
- ~~T045B~~ _(superseded — tab animation is now handled inline in T043 via keyed `StyledTabContent`; `usePathname` key trick in layout no longer needed)_
- [x] T045C [P] Update `src/types/user.ts`: add `name: string` to `User`, `AuthResponse`, and `RegisterPayload` interfaces
- [x] T045D [P] Update `src/constants/strings.ts`: add `strings.auth.nameLabel` (`"Nome"`) and `strings.auth.namePlaceholder` (`"Seu nome completo"`)
- [x] T045E [US1] Update `src/app/(auth)/login/page.tsx` register form: add name input field above email — floating label, SVG person icon, bound to `regName` state, same `StyledFieldWrapper` pattern as other fields
- [x] T045F [P] Update `src/hooks/useAuth.ts`: pass `name` in `register.mutate(payload)`

**Checkpoint**: At `/login`, click "Criar conta" → form slides in with `tabEnter` animation, underline slides to right. Register → success banner appears, auto-switches to "Entrar" tab. Log in → redirected to `/`. Refresh → still authenticated. Access `/training-plan` without token → redirected to `/login`.

---

## Phase 4: User Story 2 — Home & Calendar (Priority: P2)

**Goal**: Authenticated user sees today's workout, can mark it complete/skip, and views the monthly calendar with status indicators.

**Independent Test**: Log in, view home screen showing today's exercises (or rest day indicator), tap "Marcar como concluído", see the day turn green in the calendar grid.

- [x] T046 [US2] Create `src/app/(app)/page.tsx` (Home screen). Structure: (1) dark header section (gradient background, `borderRadius: 0 0 28px 28px`) with greeting text from `strings.home.greeting(user.name ?? user.email)`, 3 `StatCard` components (streak, this-week count, total sessions) from `useProgressionSummary()` and `useMonthlyCalendar()`. (2) Content card (white surface, `borderRadius: theme.borderRadius.card`): today's day label, exercise list for today from `useTodayCalendar()` or `useTrainingSheet()`. If rest day, show rest indicator with `strings.home.todayIsRest`. (3) Action buttons "Marcar como concluído" / "Pular hoje" at bottom of card — call `useCompleteSession()`. (4) `CalendarGrid` component below card showing current month from `useMonthlyCalendar()`. Figma reference node `1:102` (first screen, 430px frame).
      **Files to create:**
  - `src/app/(app)/home/page.tsx` — max 80 lines: hooks + layout composition only
  - `src/app/(app)/home/styles.ts` — all styled components for the Home page
  - `src/app/(app)/home/components/HomeHeader/index.tsx` — gradient header with greeting + stat cards
  - `src/app/(app)/home/components/HomeHeader/styles.ts` — HomeHeader styled components
  - `src/app/(app)/home/components/TodayPreview/index.tsx` — today’s exercises card + action buttons
  - `src/app/(app)/home/components/TodayPreview/styles.ts` — TodayPreview styled components
  - `src/app/(app)/home/components/SessionHistory/index.tsx` — monthly calendar grid + history section
  - `src/app/(app)/home/components/SessionHistory/styles.ts` — SessionHistory styled components
- [x] T047 [US2] Handle empty state in home screen: if `useTrainingSheet()` returns 404 (no sheet), render `EmptyState` with `strings.home.noSheet` and a CTA that navigates to `/training-plan`.
- [x] T048 [US2] Handle loading and error states in home screen: show skeleton placeholder (Styled Component, animated `background: linear-gradient(...)`) while data loads; show error message with retry button on API failure. Wrap entire page root with `<PageTransition>` (T041C).

**Checkpoint**: Home screen shows today's data, calendar updates after marking complete/skip.

---

## Phase 5: User Story 3 — Training Sheet Management (Priority: P3)

**Goal**: User can view, create, and configure their weekly training plan. Toggle days between training and rest.

**Independent Test**: Navigate to `/training-plan`, see 7 days of the week, toggle Monday to training, tap Monday → navigate to `/training-plan/monday`.

- [x] T049 [US3] Create `src/app/(app)/training-plan/page.tsx`: renders a list of 7 day cards (Monday–Sunday in pt-BR using `strings.trainingPlan`). Each card shows: day name, type badge (Treino / Descanso), a toggle switch to flip type via `useUpdateDay()`. If day is `training` type, shows a chevron/arrow tapping which navigates to `/training-plan/[dayOfWeek]`. If no sheet exists, renders `EmptyState` with CTA calling `useCreateTrainingSheet()`. Use `useTrainingSheet()` for data. All day names from `strings.trainingPlan.dayNames`. Wrap page root with `<PageTransition>` (T041C).
      **Files to create:**
  - `src/app/(app)/training-plan/page.tsx` — max 80 lines: hooks + layout composition only
  - `src/app/(app)/training-plan/styles.ts` — all styled components for the Training Plan page
  - `src/app/(app)/training-plan/components/PlanHeader/index.tsx` — header with training/rest day summary badges
  - `src/app/(app)/training-plan/components/PlanHeader/styles.ts` — PlanHeader styled components
  - `src/app/(app)/training-plan/components/DayCard/index.tsx` — single day row with toggle switch + chevron
  - `src/app/(app)/training-plan/components/DayCard/styles.ts` — DayCard styled components
  - `src/app/(app)/training-plan/components/MuscleOverview/index.tsx` — muscle group chips overview section
  - `src/app/(app)/training-plan/components/MuscleOverview/styles.ts` — MuscleOverview styled components
- [x] T050 [US3] Create `src/app/(app)/training-plan/[dayOfWeek]/page.tsx`: renders exercises for the selected day. Uses `useExercises(dayOfWeek)`. Shows each exercise as `ExerciseCard`. Has "Adicionar exercício" FAB or button at bottom. Shows `EmptyState` when no exercises exist. Navigates back to `/training-plan` via back button. Wrap page root with `<PageTransition>` (T041C). The "Adicionar exercício" button opens `AddExerciseModal` (T051) rendered inside the base `<Modal>` component (T041D).
- [x] T051 [US3] Implement "Add Exercise" modal form in `src/app/(app)/training-plan/[dayOfWeek]/components/AddExerciseModal.tsx`: form with exercise name input, `MuscleChip`-based muscle group selector (grid of 8 chips), and a series builder (add/remove series rows, each with type selector and reps input). Submit calls `useAddExercise(day)`. All labels from `strings.exercises`. **Must render inside the base `<Modal>` component (T041D)** — pass `isOpen` and `onClose` props through to it, do not implement a custom overlay.
- [x] T052 [US3] Implement "Edit Exercise" modal in `src/app/(app)/training-plan/[dayOfWeek]/components/EditExerciseModal.tsx`: same form pre-filled with existing exercise data. Submit calls `useUpdateExercise(day)`. Delete button triggers confirmation dialog then `useDeleteExercise(day)`.
- [x] T053 [US3] Handle loading/error states on training-plan pages: skeleton cards while loading, error state with retry button on failure.

**Checkpoint**: Create sheet → see 7 days. Toggle day type. Navigate to day → see exercises list. Add exercise → appears in list.

---

## Phase 6: User Story 4 — Exercise Management (Priority: P4)

**Goal**: Full CRUD for exercises on a training day, including series management.

**Independent Test**: Navigate to a training day, add "Supino Reto" with muscle group "Peito" and 3 working sets of 10 reps, edit the first set to 12 reps, delete the exercise.

- [x] T054 [US4] Extend `ExerciseCard` component to support inline edit/delete actions: show edit icon (pencil) and delete icon (trash) on the card. Props: `onEdit: () => void`, `onDelete: () => void`. Icons styled with `theme.colors.outline`, tap area minimum 44×44px for one-handed use. The delete confirmation triggered by `onDelete` must use the base `<Modal>` component (T041D) for its confirmation dialog rather than the native browser `confirm()`.
- [x] T055 [US4] Implement delete confirmation in `src/app/(app)/training-plan/[dayOfWeek]/page.tsx`: when user taps delete on an `ExerciseCard`, show a native browser `confirm()` or a custom styled `ConfirmModal` component. On confirm, call `useDeleteExercise(day)`. On cancel, dismiss.
- [x] T056 [US4] Implement series order UI in `AddExerciseModal` and `EditExerciseModal`: series rows must show their type (warm-up=⚡, adjustment=🎯, working=💪 or text labels from `strings.exercises.seriesTypes`), reps count, and add/remove buttons. Minimum 1 series required. Series type selection via 3-option segmented control styled with theme tokens. Both modals must already be using the base `<Modal>` component (T041D) before this task begins.

**Checkpoint**: Full exercise CRUD works. Series types selectable. Delete requires confirmation.

---

## Phase 7: User Story 5 — Workout Logging (Priority: P5)

**Goal**: User at the gym can log each set (weight, reps, rest time) and mark the session complete or skip it.

**Independent Test**: Navigate to `/train`, see today's exercises and sets, tap a pending set, enter weight=80kg, reps=10, rest=90s, save — set turns green. Tap "Finalizar Treino" — session marked complete.

- [x] T057 [US5] Create `src/app/(app)/train/page.tsx`: on mount, call `useTodaySession()`. If 404 (no session), auto-call `useCreateSession()` with today's date (ISO string). Render exercises from `useTodayCalendar()` grouped with their logged sets from the session. Each exercise section header shows name + `MuscleChip`. Below it, a list of `LoggedSetRow` components. If today is a rest day, show rest day message with option to skip session. All strings from `strings.workout`.
      **Files to create:**
  - `src/app/(app)/train/page.tsx` — max 80 lines: hooks + layout composition only
  - `src/app/(app)/train/styles.ts` — all styled components for the Workout page
  - `src/app/(app)/train/components/WorkoutHeader/index.tsx` — header with session status badge
  - `src/app/(app)/train/components/WorkoutHeader/styles.ts` — WorkoutHeader styled components
  - `src/app/(app)/train/components/ExerciseSection/index.tsx` — collapsible exercise accordion with set rows
  - `src/app/(app)/train/components/ExerciseSection/styles.ts` — ExerciseSection styled components
  - `src/app/(app)/train/components/SessionDoneBanner/index.tsx` — completion/skip banner
  - `src/app/(app)/train/components/SessionDoneBanner/styles.ts` — SessionDoneBanner styled components
  - `src/app/(app)/train/components/RestDayCard/index.tsx` — rest day display card with skip option
  - `src/app/(app)/train/components/RestDayCard/styles.ts` — RestDayCard styled components
- [x] T058 [US5] Create `src/app/(app)/train/components/LoggedSetRow.tsx`: displays one set from the plan alongside its logged state. Props: `series: Series`, `loggedSet?: LoggedSet`, `sessionId: string`, `onLog: () => void`. If `loggedSet` exists, show it as read-only (weight, reps, rest) with a green checkmark. If not, show a pending state (tap to open log form). Uses `theme.colors.successContainer` for logged background, `theme.colors.surface` for pending.
- [x] T059 [US5] Create `src/app/(app)/train/components/LogSetForm.tsx`: form shown inline or in a bottom sheet when user taps a pending set. Fields: weight (number input, step 0.5, min 0.5), reps (number input, min 0), rest time in seconds (number input, min 0). Submit calls `useAddRecord(sessionId)` with `{ exerciseName, seriesType, seriesOrder, weight, repsCompleted, restTime }`. All labels from `strings.workout`. Large tap targets (min 48px height inputs) for one-handed gym use.
- [x] T060 [US5] Implement session action buttons in `src/app/(app)/train/page.tsx`: "Finalizar Treino" button calls `useCompleteSession(sessionId)` with `{ status: 'completed' }`. "Pular sessão" button calls `useCompleteSession(sessionId)` with `{ status: 'skipped' }`. Both buttons styled as pills (`borderRadius: theme.borderRadius.pill`). After completion, show read-only summary state: all sets displayed, no edit actions, session status badge at top.
- [x] T061 [US5] Handle already-completed session in `src/app/(app)/train/page.tsx`: if `session.status === 'completed' || 'skipped'`, render full read-only summary with all logged sets and a completion badge. No edit controls visible. Show timestamp of completion. Page root must be wrapped with `<PageTransition>` (T041C) — apply to the outermost element of the `train/page.tsx` component.

**Checkpoint**: Log a set → appears green. Log all sets → session shows full progress. Tap Finalizar → read-only summary state.

---

## Phase 8: User Story 6 — Progression Tracking (Priority: P6)

**Goal**: User views weight/reps evolution chart for any exercise and sees overall training summary stats.

**Independent Test**: Navigate to `/progress`, see summary cards (streak, total sessions), select "Supino Reto" from a list, see a line/bar chart with data points from past sessions.

- [x] T062 [US6] Create `src/app/(app)/progress/page.tsx`: (1) Header section (gradient background matching Figma) with 3 summary stat cards using `StatCard` from `useProgressionSummary()`: current streak, total sessions, personal record count. (2) Exercise selector: list or dropdown of exercise names available in the user's training sheet (from `useTrainingSheet()`, flatten all exercises, deduplicate names). (3) Chart section: when exercise selected, render `ProgressionChart` component with data from `useProgressionChart(exerciseName)`. (4) `EmptyState` when no history exists or no exercise selected. All strings from `strings.progression`.
      **Files to create:**
  - `src/app/(app)/progress/page.tsx` — max 80 lines: hooks + layout composition only
  - `src/app/(app)/progress/styles.ts` — all styled components for the Progress page
  - `src/app/(app)/progress/components/ProgressHeader/index.tsx` — gradient header with summary stat cards
  - `src/app/(app)/progress/components/ProgressHeader/styles.ts` — ProgressHeader styled components
  - `src/app/(app)/progress/components/WeeklyActivityChart/index.tsx` — bar chart for weekly training activity
  - `src/app/(app)/progress/components/WeeklyActivityChart/styles.ts` — WeeklyActivityChart styled components
  - `src/app/(app)/progress/components/ProgressionChart/index.tsx` — composed line+bar chart for load progression
  - `src/app/(app)/progress/components/ProgressionChart/styles.ts` — ProgressionChart styled components
  - `src/app/(app)/progress/components/MuscleFocusSection/index.tsx` — horizontal bar rows per muscle group
  - `src/app/(app)/progress/components/MuscleFocusSection/styles.ts` — MuscleFocusSection styled components
- [x] T063 [US6] Create `src/app/(app)/progress/components/ProgressionChart.tsx`: Recharts `ComposedChart` with a `Line` for weight (left Y-axis, color `theme.colors.primary`) and `Bar` for reps completed (right Y-axis, color `theme.colors.primaryContainer`). X-axis: formatted dates (DD/MM). Responsive via Recharts `ResponsiveContainer` width="100%". No hardcoded colors — pass theme values as props. Tooltip shows date, weight (kg), reps. Empty state when `data.length === 0`.
- [x] T064 [US6] Handle loading/error/empty states on progress screen: skeleton placeholder while data loads, error state with retry, welcoming `EmptyState` with guidance copy from `strings.progression.empty` when no sessions logged yet.

**Checkpoint**: Progress screen shows summary. Select exercise → chart renders with data points. No data → empty state with guidance.

---

## Phase 9: Polish & Cross-Cutting Concerns

**Purpose**: Responsiveness, accessibility, theme toggle, and global UX consistency.

- [x] T065 [P] Add responsive breakpoints to all page layouts in `src/app/(app)/`: at 768px (`min-width`) center content in a max-width container (e.g., `max-width: 430px; margin: 0 auto`) so the mobile layout is preserved on desktop. Apply to all `(app)/*` pages. Ensure 320px minimum width renders without horizontal scroll.
- [x] T066 [P] Add theme toggle button: create `src/components/ThemeToggle/index.tsx` (sun/moon icon button). Place it in the root layout header or as a floating button visible on all screens. On press call `useTheme().toggleTheme()`. Persist in `localStorage` key `loadup_theme` (already handled by `ThemeProvider`). Styled with `theme.colors.primary`, `borderRadius: theme.borderRadius.pill`.
- [x] T067 [P] Audit all pages and components for hardcoded strings: verify every user-facing string is consumed from `strings.ts`. Fix any hardcoded text found.
- [x] T068 [P] Audit all styled components for hardcoded hex values or pixel sizes outside of theme tokens: replace any found with the appropriate `theme.*` token reference.
- [x] T069 Update `src/app/globals.css` to contain only: `*, *::before, *::after { box-sizing: border-box; }` and `html { -webkit-tap-highlight-color: transparent; }` — all other global styles handled by `GlobalStyles` Styled Component from T010.

**Checkpoint**: App renders correctly at 320px and 1280px. Theme toggle works and persists. No hardcoded strings or colors remain.

---

## Phase 10: Architecture Validation

**Purpose**: Verify that all implemented pages and components comply with the strict file separation architecture defined in `spec.md` and `plan.md`. Run this phase as a final quality gate before delivery.

- [x] T070 [P] Audit `src/app/(auth)/login/`: verify `page.tsx with no styled components and no inline component declarations; `styles.ts`exists; each sub-component in`components/`is a folder with`index.tsx`+`styles.ts`. Fix any violations.
- [x] T071 [P] Audit `src/app/(app)/home/`: same rules as T070. Verify `HomeHeader`, `TodayPreview`, `SessionHistory` each have their own folder under `components/`. Fix any violations.
- [x] T072 [P] Audit `src/app/(app)/training-plan/`: same rules as T070. Verify `PlanHeader`, `DayCard`, `MuscleOverview` each have their own folder under `components/`. Fix any violations.
- [x] T073 [P] Audit `src/app/(app)/training-plan/[dayOfWeek]/`: same rules as T070. Verify `DayHeader`, `ExerciseList`, `AddExerciseModal`, `EditExerciseModal`, `DeleteConfirmModal` each have their own folder under `components/`. Fix any violations.
- [x] T074 [P] Audit `src/app/(app)/train/`: same rules as T070. Verify `WorkoutHeader`, `ExerciseSection`, `LoggedSetRow`, `LogSetForm`, `SessionDoneBanner`, `RestDayCard` each have their own folder under `components/`. Fix any violations.
- [x] T075 [P] Audit `src/app/(app)/progress/`: same rules as T070. Verify `ProgressHeader`, `WeeklyActivityChart`, `ProgressionChart`, `MuscleFocusSection` each have their own folder under `components/`. Fix any violations.
- [x] T076 Run `npx tsc --noEmit` and confirm zero TypeScript errors after all architecture refactoring is complete.

**Checkpoint**: . All styled components live in `styles.ts` files. No inline component declarations in any `page.tsx`. Zero TypeScript errors.

---

## Dependency Graph (User Story Completion Order)

```
Phase 1 (Setup)
    └── Phase 2 (Foundation: T008–T041)
             ├── Phase 3 (US1 Auth: T042–T044)      ← MVP
             │       └── Phase 4 (US2 Home: T046–T048)
             │               └── Phase 5 (US3 Sheet: T049–T053)
             │                       └── Phase 6 (US4 Exercises: T054–T056)
             │                               └── Phase 7 (US5 Workout: T057–T061)
             │                                       └── Phase 8 (US6 Progress: T062–T064)
             └── Phase 9 (Polish: T065–T069)  ← parallel after Phase 2
                      └── Phase 10 (Validation: T070–T076)  ← after all phases complete
```

## Parallel Execution Within Phases

| Phase    | Parallelizable tasks                                     |
| -------- | -------------------------------------------------------- |
| Phase 2B | T012, T013, T014                                         |
| Phase 2C | T015, T016, T017, T018, T019 (T020 depends on all)       |
| Phase 2E | T022, T023, T024, T025, T026, T027, T028                 |
| Phase 2F | T029, T030, T031, T032, T033, T034                       |
| Phase 2H | T036, T037, T038, T039, T040, T041, T041E, T042          |
| Phase 9  | T065, T066, T067, T068                                   |
| Phase 10 | T070, T071, T072, T073, T074, T075 (T076 depends on all) |

## Implementation Strategy

**MVP scope**: Phases 1–3 (T001–T045). After completing Phase 3, a user can register, log in, and reach the home screen. All subsequent phases add independent value.

**Recommended delivery order**:

1. Phases 1–2 as a single sprint (foundation — no visible UI yet)
2. Phase 3 (auth — first visible UI, minimal viable product entry point)
3. Phases 4–5 (home + training sheet — daily-use core)
4. Phases 6–7 (exercises + workout logging — gym core)
5. Phase 8 (progression — analytics layer)
6. Phase 9 (polish — final quality pass)
7. Phase 10 (Validation)
